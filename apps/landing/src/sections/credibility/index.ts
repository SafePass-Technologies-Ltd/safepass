/**
 * The shared Credibility & Safety-Data module (FEAT-005).
 *
 * Import from here, not from the files directly, so the four placements named
 * in screens.md's Cross-Screen Notes all resolve to one implementation.
 */
export { CredibilitySection, CredibilityPreview } from './credibility-section';
export { StatCallout } from './stat-callout';
